<Page
    x:Class="KaiYan.Pages.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KaiYan.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel VerticalAlignment="Center" Width="300">

            <TextBlock Text="开眼账号登录" HorizontalAlignment="Center" FontSize="20"/>

            <TextBlock Text="账号："/>
            <TextBox x:Name="usernameTextBox" />
            <TextBlock Text="密码：" Margin="0,10,0,10" />
            <PasswordBox x:Name="passwordTextBox" />
            <CheckBox x:Name="remindCheckBox" VerticalAlignment="Center" Content="保存密码" Margin="0,10,0,10" />
            <Button Background="{ThemeResource SystemAccentColor}" Click="Button_Click"
                    Content="登录" Margin="0,10,0,10" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Stretch"/>
        </StackPanel>

    </Grid>
</Page>
